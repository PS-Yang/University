設計一個hill Climbing 搜尋法的程式以解決8-puzzle問題。 

輸入: 

由鍵盤鍵入起始狀態(initial state)與目標狀態(goal state) 

如 540618732 123804765，其中0代表空格位置。 

輸出：
從初始狀態到目的狀態經過的中間狀態與步數(若不可到達則輸出無法到達)與執行時間。 

啟發式函數f(n)=d(n)+w(n)，如課本相同。 

要求： 
1. 能偵測輸入錯誤， 
2. 目標狀態可以改變，
3. 輸出目前處理節點於搜尋樹中的兒子節點與評估函數之值(包含f(n), d(n), w(n)) 